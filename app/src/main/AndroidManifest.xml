<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.hometask1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.HomeTask1Activity" />
        <activity android:name=".Activities.HomeTask2Activity" />
        <activity android:name=".Activities.HomeTask3Activity" />
        <activity android:name=".Activities.HomeTask4Activity" />
        <activity android:name=".Activities.HomeTask5Activity" />

        <service android:name=".LocalService" />

        <activity android:name=".Activities.HomeTask6Activity" />
        <activity android:name=".Activities.StudentActivity" />
        <activity android:name=".Activities.AddStudentActivity" />
    </application>

</manifest>